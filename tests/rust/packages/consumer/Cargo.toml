[package]
name = "consumer"
version = "0.21.0"
edition = "2024"

[dependencies]
test = { version = "0.0.1", path = "../.." }
other = "1.0.0"

[dev-dependencies]
test = "0.0.1"

[build-dependencies]
test = { version = "0.0.1" }
